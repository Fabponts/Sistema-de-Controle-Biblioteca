/*package atributes;
import java.util.List;
import java.util.ArrayList;

public class Library {

    private List<Book> books;  // Lista de livros disponíveis na biblioteca
    private String bookOptions;

    public Library() {
        this.books = new ArrayList<>();  // Inicializa a lista de livros
    }

    // Método para adicionar um livro à biblioteca
    public void addBook(Book book) {
        books.add(book);
        System.out.println("The book '" + book.getTitle() + "' was added to the library.");
    }
}
 */
